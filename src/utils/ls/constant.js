//handelling after recieving token from  spotify web api

// export const storeToken=(access_token,refresh_token)=>
// {
//     localStorage.setItem("access_token",access_token);
//     localStorage.setItem("refresh_token",refresh_token);
    
//     //expiry_time
 
//     const expiry_In=3600;
//     const expiry_time= new Date().getTime() + expiry_In * 1000
//     localStorage.setItem('expiry_time',expiry_time);

// } 